//Hide Warning
$(".warning").hide();
//Show Warning Slowly
$(".warning").show("slow");

// If jquery is loaded first, loading at bottom of page is best practice as we can assume the DOM has been loaded
// function myCode() {
//     $(".warning").hide().show("slow");
// }

// $(document).ready(myCode);
